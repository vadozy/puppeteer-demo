{
  "name": "puppeteer-demo",
  "version": "1.0.0",
  "private": true,
  "description": "puppeteer demo",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watchAll",
    "test:watch:coverage": "jest --watchAll --coverage --coverageReporters=text",
    "format:lint": "prettier --write '**/*.js' && eslint --fix ."
  },
  "author": "Vadim Storozhuk",
  "license": "ISC",
  "devDependencies": {
    "babel-eslint": "^10.0.3",
    "eslint": "^6.6.0",
    "eslint-plugin-jest": "^23.0.2",
    "jest": "^24.9.0",
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "puppeteer": "^2.0.0"
  }
}
